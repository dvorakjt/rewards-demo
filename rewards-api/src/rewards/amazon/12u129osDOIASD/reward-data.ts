import type { IRewardData } from "../../../model/reward-data";

/* 
  Need to compile this file and dynamically import it
*/
export const rewardData: IRewardData = {
  shortDescription: "",
  redemptionForums: [],
  expirationDate: new Date(),
};
