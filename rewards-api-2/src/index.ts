// for now, let's just think of this as the build script

// synchronize the database
// copy partner logos (or create symlinks) into public directory
// start the server

// we need a voucher type, a claim function type, redemption methods type,
// then i will need functions that the control panel can call
